apiVersion: v1
kind: Service
metadata:
  name: prismpipe
  namespace: prismpipe
  labels:
    app.kubernetes.io/name: prismpipe
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: prismpipe
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
